--- cmake/pkg-config.cmake~	2016-10-10 14:15:08.236334161 +0200
+++ cmake/pkg-config.cmake	2016-10-10 14:16:17.905040532 +0200
@@ -648,6 +648,7 @@
       LIST(APPEND CFLAGS "${FLAG}")
     ENDFOREACH()
   ENDIF()
+  STRING(REPLACE ";" " " CFLAGS "${CFLAGS}") # This cast from LIST to STRING is mandatory for old CMake versions
   SET_TARGET_PROPERTIES(${TARGET} PROPERTIES ${COMPILE_OPTIONS_NAME} "${CFLAGS}")
 
   # Include/libraries paths seems to be filtered on Linux, add paths
